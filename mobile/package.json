{
  "name": "mobile",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --check ../app.js && node --check scripts/copy-assets.js",
    "copy": "node scripts/copy-assets.js",
    "sync": "npm run copy && cap sync android",
    "android:debug": "npm run sync && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run sync && cd android && gradlew.bat assembleRelease",
    "android:open": "npm run sync && cap open android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
